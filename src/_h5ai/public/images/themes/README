Themes

This directory will contain any themes you add. At the moment there are only icon themes supported.
The folder structure is: <theme-name>/<type>.<ext>, With <ext> one of svg png jpg.
To select a theme use the option `view > theme` in file `conf/options.json`.
